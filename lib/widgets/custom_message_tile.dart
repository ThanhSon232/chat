import 'package:flutter/cupertino.dart';

class CustomMessageTile extends StatelessWidget {
  const CustomMessageTile({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
